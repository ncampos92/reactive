{% extends 'base.html.twig' %}

{% block body %}
    <h1>Formanswer creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('appform_formanswer_index', { 'appform_id': appform_id }) }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
